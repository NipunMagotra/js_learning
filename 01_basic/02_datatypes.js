"use strict"; // it helps to treat all the JS code as the newer version.

alert(3+6); // it will show the output in the alert box , but in browser not in the console.

// few examples of the data types
let name="nipun"
let age =20
let isSignedIn= true

//examples of data types javascript = number, string, boolean, undefined, null-> standalone value, 
//symbol->for uniqueness, object
